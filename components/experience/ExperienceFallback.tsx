"use client";

export function ExperienceFallback() {
  return (
    <div className="experience-fallback" role="status">
      <span>2D SAFE MODE</span>
      <p>The 3D environment is unavailable. Every portfolio section remains accessible below.</p>
    </div>
  );
}
